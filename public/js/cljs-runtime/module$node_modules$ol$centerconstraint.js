shadow$provide.module$node_modules$ol$centerconstraint = function(global, require, module, exports) {
  function createExtent(extent, onlyCenter, smooth) {
    return function(center, resolution, size, isMoving, centerShift) {
      if (center) {
        if (!resolution && !onlyCenter) {
          return center;
        }
        var viewWidth = onlyCenter ? 0 : size[0] * resolution, viewHeight = onlyCenter ? 0 : size[1] * resolution;
        size = centerShift ? centerShift[0] : 0;
        var shiftY = centerShift ? centerShift[1] : 0;
        centerShift = extent[0] + viewWidth / 2 + size;
        viewWidth = extent[2] - viewWidth / 2 + size;
        size = extent[1] + viewHeight / 2 + shiftY;
        viewHeight = extent[3] - viewHeight / 2 + shiftY;
        centerShift > viewWidth && (viewWidth = centerShift = (viewWidth + centerShift) / 2);
        size > viewHeight && (viewHeight = size = (viewHeight + size) / 2);
        shiftY = (0,require$_DOT__SLASH_math_DOT_js.clamp)(center[0], centerShift, viewWidth);
        var y = (0,require$_DOT__SLASH_math_DOT_js.clamp)(center[1], size, viewHeight);
        isMoving && smooth && resolution && (resolution *= 30, shiftY += -resolution * Math.log(1 + Math.max(0, centerShift - center[0]) / resolution) + resolution * Math.log(1 + Math.max(0, center[0] - viewWidth) / resolution), y += -resolution * Math.log(1 + Math.max(0, size - center[1]) / resolution) + resolution * Math.log(1 + Math.max(0, center[1] - viewHeight) / resolution));
        return [shiftY, y];
      }
    };
  }
  function none(center) {
    return center;
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, createExtent:{enumerable:!0, get:function() {
    return createExtent;
  }}, none:{enumerable:!0, get:function() {
    return none;
  }}});
  var require$_DOT__SLASH_math_DOT_js = require("module$node_modules$ol$math");
};

//# sourceMappingURL=module$node_modules$ol$centerconstraint.js.map
