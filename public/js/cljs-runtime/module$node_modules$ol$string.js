shadow$provide.module$node_modules$ol$string = function(global, require, module, exports) {
  function padNumber(number, width, precision) {
    number = void 0 !== precision ? number.toFixed(precision) : "" + number;
    precision = number.indexOf(".");
    precision = -1 === precision ? number.length : precision;
    return precision > width ? number : Array(1 + width - precision).join("0") + number;
  }
  function compareVersions(v1, v2) {
    v1 = ("" + v1).split(".");
    v2 = ("" + v2).split(".");
    for (let i = 0; i < Math.max(v1.length, v2.length); i++) {
      const n1 = parseInt(v1[i] || "0", 10), n2 = parseInt(v2[i] || "0", 10);
      if (n1 > n2) {
        return 1;
      }
      if (n2 > n1) {
        return -1;
      }
    }
    return 0;
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, compareVersions:{enumerable:!0, get:function() {
    return compareVersions;
  }}, padNumber:{enumerable:!0, get:function() {
    return padNumber;
  }}});
};

//# sourceMappingURL=module$node_modules$ol$string.js.map
