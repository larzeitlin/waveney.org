shadow$provide.module$node_modules$ol$geom$flat$area = function(global, require, module, exports) {
  function linearRing(flatCoordinates, offset, end, stride) {
    let twiceArea = 0;
    const x0 = flatCoordinates[end - stride], y0 = flatCoordinates[end - stride + 1];
    let dx1 = 0, dy1 = 0;
    for (; offset < end; offset += stride) {
      const dx2 = flatCoordinates[offset] - x0, dy2 = flatCoordinates[offset + 1] - y0;
      twiceArea += dy1 * dx2 - dx1 * dy2;
      dx1 = dx2;
      dy1 = dy2;
    }
    return twiceArea / 2;
  }
  function linearRings(flatCoordinates, offset, ends, stride) {
    let area = 0;
    for (let i = 0, ii = ends.length; i < ii; ++i) {
      const end = ends[i];
      area += linearRing(flatCoordinates, offset, end, stride);
      offset = end;
    }
    return area;
  }
  function linearRingss(flatCoordinates, offset, endss, stride) {
    let area = 0;
    for (let i = 0, ii = endss.length; i < ii; ++i) {
      const ends = endss[i];
      area += linearRings(flatCoordinates, offset, ends, stride);
      offset = ends[ends.length - 1];
    }
    return area;
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, linearRing:{enumerable:!0, get:function() {
    return linearRing;
  }}, linearRings:{enumerable:!0, get:function() {
    return linearRings;
  }}, linearRingss:{enumerable:!0, get:function() {
    return linearRingss;
  }}});
};

//# sourceMappingURL=module$node_modules$ol$geom$flat$area.js.map
