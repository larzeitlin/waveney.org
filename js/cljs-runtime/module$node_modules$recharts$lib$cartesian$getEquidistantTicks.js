shadow$provide.module$node_modules$recharts$lib$cartesian$getEquidistantTicks = function(global, require, module, exports) {
  Object.defineProperty(exports, "__esModule", {value:!0});
  exports.getEquidistantTicks = function(sign, boundaries, getTickSize, ticks, minTickGap) {
    var result = (ticks || []).slice(), initialStart = boundaries.start, end = boundaries.end, index = 0, stepsize = 1, start = initialStart;
    boundaries = function() {
      var entry = null === ticks || void 0 === ticks ? void 0 : ticks[index];
      if (void 0 === entry) {
        return {v:(0,_getEveryNthWithCondition.getEveryNthWithCondition)(ticks, stepsize)};
      }
      var i = index, size, getSize = function() {
        void 0 === size && (size = getTickSize(entry, i));
        return size;
      }, tickCoord = entry.coordinate, isShow = 0 === index || (0,_TickUtils.isVisible)(sign, tickCoord, getSize, start, end);
      isShow || (index = 0, start = initialStart, stepsize += 1);
      isShow && (start = tickCoord + sign * (getSize() / 2 + minTickGap), index += stepsize);
    };
    for (var _ret; stepsize <= result.length;) {
      if (_ret = boundaries()) {
        return _ret.v;
      }
    }
    return [];
  };
  var _TickUtils = require("module$node_modules$recharts$lib$util$TickUtils"), _getEveryNthWithCondition = require("module$node_modules$recharts$lib$util$getEveryNthWithCondition");
};

//# sourceMappingURL=module$node_modules$recharts$lib$cartesian$getEquidistantTicks.js.map
