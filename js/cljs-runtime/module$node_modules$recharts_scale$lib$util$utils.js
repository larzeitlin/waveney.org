shadow$provide.module$node_modules$recharts_scale$lib$util$utils = function(global, require, module, exports) {
  function _toConsumableArray(arr) {
    var JSCompiler_temp = Array.isArray(arr) ? _arrayLikeToArray(arr) : void 0;
    JSCompiler_temp || (JSCompiler_temp = "undefined" !== typeof Symbol && Symbol.iterator in Object(arr) ? Array.from(arr) : void 0);
    if (!JSCompiler_temp) {
      a: {
        if (arr) {
          if ("string" === typeof arr) {
            JSCompiler_temp = _arrayLikeToArray(arr, void 0);
            break a;
          }
          JSCompiler_temp = Object.prototype.toString.call(arr).slice(8, -1);
          "Object" === JSCompiler_temp && arr.constructor && (JSCompiler_temp = arr.constructor.name);
          if ("Map" === JSCompiler_temp || "Set" === JSCompiler_temp) {
            JSCompiler_temp = Array.from(arr);
            break a;
          }
          if ("Arguments" === JSCompiler_temp || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(JSCompiler_temp)) {
            JSCompiler_temp = _arrayLikeToArray(arr, void 0);
            break a;
          }
        }
        JSCompiler_temp = void 0;
      }
    }
    if (!(arr = JSCompiler_temp)) {
      throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
    }
    return arr;
  }
  function _arrayLikeToArray(arr, len) {
    if (null == len || len > arr.length) {
      len = arr.length;
    }
    for (var i = 0, arr2 = Array(len); i < len; i++) {
      arr2[i] = arr[i];
    }
    return arr2;
  }
  Object.defineProperty(exports, "__esModule", {value:!0});
  exports.memoize = exports.reverse = exports.compose = exports.map = exports.range = exports.curry = exports.PLACE_HOLDER = void 0;
  var identity = function(i) {
    return i;
  }, PLACE_HOLDER = {"@@functional/placeholder":!0};
  exports.PLACE_HOLDER = PLACE_HOLDER;
  var curry0 = function(fn) {
    return function _curried() {
      return 0 === arguments.length || 1 === arguments.length && (0 >= arguments.length ? void 0 : arguments[0]) === PLACE_HOLDER ? _curried : fn.apply(void 0, arguments);
    };
  }, curryN = function curryN(n, fn) {
    return 1 === n ? fn : curry0(function() {
      for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
        args[_key] = arguments[_key];
      }
      _len = args.filter(function(arg) {
        return arg !== PLACE_HOLDER;
      }).length;
      return _len >= n ? fn.apply(void 0, args) : curryN(n - _len, curry0(function() {
        for (var _len2 = arguments.length, restArgs = Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
          restArgs[_key2] = arguments[_key2];
        }
        _len2 = args.map(function(arg) {
          return arg === PLACE_HOLDER ? restArgs.shift() : arg;
        });
        return fn.apply(void 0, _toConsumableArray(_len2).concat(restArgs));
      }));
    });
  };
  global = function(fn) {
    return curryN(fn.length, fn);
  };
  exports.curry = global;
  exports.range = function(begin, end) {
    for (var arr = [], i = begin; i < end; ++i) {
      arr[i - begin] = i;
    }
    return arr;
  };
  global = global(function(fn, arr) {
    return Array.isArray(arr) ? arr.map(fn) : Object.keys(arr).map(function(key) {
      return arr[key];
    }).map(fn);
  });
  exports.map = global;
  exports.compose = function() {
    for (var _len3 = arguments.length, args = Array(_len3), _key3 = 0; _key3 < _len3; _key3++) {
      args[_key3] = arguments[_key3];
    }
    if (!args.length) {
      return identity;
    }
    _len3 = args.reverse();
    var firstFn = _len3[0], tailsFn = _len3.slice(1);
    return function() {
      return tailsFn.reduce(function(res, fn) {
        return fn(res);
      }, firstFn.apply(void 0, arguments));
    };
  };
  exports.reverse = function(arr) {
    return Array.isArray(arr) ? arr.reverse() : arr.split("").reverse.join("");
  };
  exports.memoize = function(fn) {
    var lastArgs = null, lastResult = null;
    return function() {
      for (var _len4 = arguments.length, args = Array(_len4), _key4 = 0; _key4 < _len4; _key4++) {
        args[_key4] = arguments[_key4];
      }
      if (lastArgs && args.every(function(val, i) {
        return val === lastArgs[i];
      })) {
        return lastResult;
      }
      lastArgs = args;
      return lastResult = fn.apply(void 0, args);
    };
  };
};

//# sourceMappingURL=module$node_modules$recharts_scale$lib$util$utils.js.map
