shadow$provide.module$node_modules$react_smooth$lib$easing = function(global, require, module, exports) {
  function _slicedToArray(arr, i$jscomp$0) {
    var JSCompiler_temp = Array.isArray(arr) ? arr : void 0;
    if (!JSCompiler_temp) {
      a: {
        var t = null == arr ? null : "undefined" != typeof Symbol && arr[Symbol.iterator] || arr["@@iterator"];
        if (null != t) {
          var e, i, u, a = [], f = !0, o = !1;
          try {
            if (i = (t = t.call(arr)).next, 0 === i$jscomp$0) {
              if (Object(t) !== t) {
                JSCompiler_temp = void 0;
                break a;
              }
              f = !1;
            } else {
              for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== i$jscomp$0); f = !0) {
              }
            }
          } catch (r) {
            o = !0;
            var n = r;
          } finally {
            try {
              if (!f && null != t.return && (u = t.return(), Object(u) !== u)) {
                JSCompiler_temp = void 0;
                break a;
              }
            } finally {
              if (o) {
                throw n;
              }
            }
          }
          JSCompiler_temp = a;
        } else {
          JSCompiler_temp = void 0;
        }
      }
    }
    if (!(arr = JSCompiler_temp || _unsupportedIterableToArray(arr, i$jscomp$0))) {
      throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
    }
    return arr;
  }
  function _toConsumableArray(arr) {
    var JSCompiler_temp = Array.isArray(arr) ? _arrayLikeToArray(arr) : void 0;
    JSCompiler_temp || (JSCompiler_temp = "undefined" !== typeof Symbol && null != arr[Symbol.iterator] || null != arr["@@iterator"] ? Array.from(arr) : void 0);
    if (!(arr = JSCompiler_temp || _unsupportedIterableToArray(arr))) {
      throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
    }
    return arr;
  }
  function _unsupportedIterableToArray(o, minLen) {
    if (o) {
      if ("string" === typeof o) {
        return _arrayLikeToArray(o, minLen);
      }
      var n = Object.prototype.toString.call(o).slice(8, -1);
      "Object" === n && o.constructor && (n = o.constructor.name);
      if ("Map" === n || "Set" === n) {
        return Array.from(o);
      }
      if ("Arguments" === n || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) {
        return _arrayLikeToArray(o, minLen);
      }
    }
  }
  function _arrayLikeToArray(arr, len) {
    if (null == len || len > arr.length) {
      len = arr.length;
    }
    for (var i = 0, arr2 = Array(len); i < len; i++) {
      arr2[i] = arr[i];
    }
    return arr2;
  }
  Object.defineProperty(exports, "__esModule", {value:!0});
  exports.configSpring = exports.configEasing = exports.configBezier = void 0;
  var _util = require("module$node_modules$react_smooth$lib$util"), multyTime = function(params, t) {
    return params.map(function(param, i) {
      return param * Math.pow(t, i);
    }).reduce(function(pre, curr) {
      return pre + curr;
    });
  }, cubicBezier = function(c1, c2) {
    return function(t) {
      return multyTime([0, 3 * c1, 3 * c2 - 6 * c1, 3 * c1 - 3 * c2 + 1], t);
    };
  }, derivativeCubicBezier = function(c1, c2) {
    return function(t) {
      var newParams = [].concat(_toConsumableArray([0, 3 * c1, 3 * c2 - 6 * c1, 3 * c1 - 3 * c2 + 1].map(function(param, i) {
        return param * i;
      }).slice(1)), [0]);
      return multyTime(newParams, t);
    };
  }, configBezier = exports.configBezier = function() {
    for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    _len = args[0];
    _key = args[1];
    var x2 = args[2], y2 = args[3];
    if (1 === args.length) {
      switch(args[0]) {
        case "linear":
          _key = _len = 0.0;
          y2 = x2 = 1.0;
          break;
        case "ease":
          _len = 0.25;
          _key = 0.1;
          x2 = 0.25;
          y2 = 1.0;
          break;
        case "ease-in":
          _len = 0.42;
          _key = 0.0;
          y2 = x2 = 1.0;
          break;
        case "ease-out":
          _len = 0.42;
          _key = 0.0;
          x2 = 0.58;
          y2 = 1.0;
          break;
        case "ease-in-out":
          _key = _len = 0.0;
          x2 = 0.58;
          y2 = 1.0;
          break;
        default:
          var easing = args[0].split("(");
          "cubic-bezier" === easing[0] && 4 === easing[1].split(")")[0].split(",").length ? (_len = easing[1].split(")")[0].split(",").map(function(x) {
            return parseFloat(x);
          }), y2 = _slicedToArray(_len, 4), _len = y2[0], _key = y2[1], x2 = y2[2], y2 = y2[3]) : (0,_util.warn)(!1, "[configBezier]: arguments should be one of oneOf 'linear', 'ease', 'ease-in', 'ease-out', 'ease-in-out','cubic-bezier(x1,y1,x2,y2)', instead received %s", args);
      }
    }
    (0,_util.warn)([_len, x2, _key, y2].every(function(num) {
      return "number" === typeof num && 0 <= num && 1 >= num;
    }), "[configBezier]: arguments should be x1, y1, x2, y2 of [0, 1] instead received %s", args);
    var curveX = cubicBezier(_len, x2), curveY = cubicBezier(_key, y2), derCurveX = derivativeCubicBezier(_len, x2);
    args = function(_t) {
      for (var x = _t = 1 < _t ? 1 : _t, i = 0; 8 > i; ++i) {
        var evalT = curveX(x) - _t, derVal = derCurveX(x);
        if (1e-4 > Math.abs(evalT - _t) || 1e-4 > derVal) {
          break;
        }
        x -= evalT / derVal;
        x = 1 < x ? 1 : 0 > x ? 0 : x;
      }
      return curveY(x);
    };
    args.isStepper = !1;
    return args;
  }, configSpring = exports.configSpring = function() {
    var config = 0 < arguments.length && void 0 !== arguments[0] ? arguments[0] : {}, _config$stiff = config.stiff, stiff = void 0 === _config$stiff ? 100 : _config$stiff;
    _config$stiff = config.damping;
    var damping = void 0 === _config$stiff ? 8 : _config$stiff;
    config = config.dt;
    var dt = void 0 === config ? 17 : config;
    config = function(currX, destX, currV) {
      var newV = currV + (-(currX - destX) * stiff - currV * damping) * dt / 1000;
      currX = currV * dt / 1000 + currX;
      return 1e-4 > Math.abs(currX - destX) && 1e-4 > Math.abs(newV) ? [destX, 0] : [currX, newV];
    };
    config.isStepper = !0;
    config.dt = dt;
    return config;
  };
  exports.configEasing = function() {
    for (var _len2 = arguments.length, args = Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
      args[_key2] = arguments[_key2];
    }
    _len2 = args[0];
    if ("string" === typeof _len2) {
      switch(_len2) {
        case "ease":
        case "ease-in-out":
        case "ease-out":
        case "ease-in":
        case "linear":
          return configBezier(_len2);
        case "spring":
          return configSpring();
        default:
          if ("cubic-bezier" === _len2.split("(")[0]) {
            return configBezier(_len2);
          }
          (0,_util.warn)(!1, "[configEasing]: first argument should be one of 'ease', 'ease-in', 'ease-out', 'ease-in-out','cubic-bezier(x1,y1,x2,y2)', 'linear' and 'spring', instead  received %s", args);
      }
    }
    if ("function" === typeof _len2) {
      return _len2;
    }
    (0,_util.warn)(!1, "[configEasing]: first argument type should be function or string, instead received %s", args);
    return null;
  };
};

//# sourceMappingURL=module$node_modules$react_smooth$lib$easing.js.map
