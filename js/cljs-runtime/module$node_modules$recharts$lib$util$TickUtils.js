shadow$provide.module$node_modules$recharts$lib$util$TickUtils = function(global, require, module, exports) {
  Object.defineProperty(exports, "__esModule", {value:!0});
  exports.getAngledTickWidth = function(contentSize, unitSize, angle) {
    return (0,_CartesianUtils.getAngledRectangleWidth)({width:contentSize.width + unitSize.width, height:contentSize.height + unitSize.height}, angle);
  };
  exports.getNumberIntervalTicks = function(ticks, interval) {
    return (0,_getEveryNthWithCondition.getEveryNthWithCondition)(ticks, interval + 1);
  };
  exports.getTickBoundaries = function(viewBox, sign, sizeKey) {
    sizeKey = "width" === sizeKey;
    var x = viewBox.x, y = viewBox.y, width = viewBox.width;
    viewBox = viewBox.height;
    return 1 === sign ? {start:sizeKey ? x : y, end:sizeKey ? x + width : y + viewBox} : {start:sizeKey ? x + width : y + viewBox, end:sizeKey ? x : y};
  };
  exports.isVisible = function(sign, tickPosition, getSize, start, end) {
    if (sign * tickPosition < sign * start || sign * tickPosition > sign * end) {
      return !1;
    }
    getSize = getSize();
    return 0 <= sign * (tickPosition - sign * getSize / 2 - start) && 0 >= sign * (tickPosition + sign * getSize / 2 - end);
  };
  var _CartesianUtils = require("module$node_modules$recharts$lib$util$CartesianUtils"), _getEveryNthWithCondition = require("module$node_modules$recharts$lib$util$getEveryNthWithCondition");
};

//# sourceMappingURL=module$node_modules$recharts$lib$util$TickUtils.js.map
