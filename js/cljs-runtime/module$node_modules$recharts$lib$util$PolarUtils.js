shadow$provide.module$node_modules$recharts$lib$util$PolarUtils = function(global, require, module, exports) {
  function _typeof(o) {
    "@babel/helpers - typeof";
    return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o) {
      return typeof o;
    } : function(o) {
      return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
    }, _typeof(o);
  }
  function _interopRequireDefault(obj) {
    return obj && obj.__esModule ? obj : {"default":obj};
  }
  function ownKeys(e, r) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r && (o = o.filter(function(r) {
        return Object.getOwnPropertyDescriptor(e, r).enumerable;
      }));
      t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread(e) {
    for (var r = 1; r < arguments.length; r++) {
      var t = null != arguments[r] ? arguments[r] : {};
      r % 2 ? ownKeys(Object(t), !0).forEach(function(r) {
        _defineProperty(e, r, t[r]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function(r) {
        Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
      });
    }
    return e;
  }
  function _defineProperty(obj, key, value) {
    a: {
      if ("object" == _typeof(key) && key) {
        var e = key[Symbol.toPrimitive];
        if (void 0 !== e) {
          key = e.call(key, "string");
          if ("object" != _typeof(key)) {
            break a;
          }
          throw new TypeError("@@toPrimitive must return a primitive value.");
        }
        key = String(key);
      }
    }
    key = "symbol" == _typeof(key) ? key : key + "";
    key in obj ? Object.defineProperty(obj, key, {value, enumerable:!0, configurable:!0, writable:!0}) : obj[key] = value;
    return obj;
  }
  function _arrayLikeToArray(arr, len) {
    if (null == len || len > arr.length) {
      len = arr.length;
    }
    for (var i = 0, arr2 = Array(len); i < len; i++) {
      arr2[i] = arr[i];
    }
    return arr2;
  }
  Object.defineProperty(exports, "__esModule", {value:!0});
  exports.radianToDegree = exports.polarToCartesian = exports.inRangeOfSector = exports.getTickClassName = exports.getMaxRadius = exports.getAngleOfPoint = exports.formatAxisMap = exports.formatAngleOfSector = exports.distanceBetweenPoints = exports.degreeToRadian = exports.RADIAN = void 0;
  var _isNil = _interopRequireDefault(require("module$node_modules$lodash$isNil")), _react = require("module$node_modules$react$index"), _isFunction = _interopRequireDefault(require("module$node_modules$lodash$isFunction")), _DataUtils = require("module$node_modules$recharts$lib$util$DataUtils"), _ChartUtils = require("module$node_modules$recharts$lib$util$ChartUtils"), RADIAN = exports.RADIAN = Math.PI / 180;
  exports.degreeToRadian = function(angle) {
    return angle * Math.PI / 180;
  };
  var radianToDegree = exports.radianToDegree = function(angleInRadian) {
    return 180 * angleInRadian / Math.PI;
  };
  exports.polarToCartesian = function(cx, cy, radius, angle) {
    return {x:cx + Math.cos(-RADIAN * angle) * radius, y:cy + Math.sin(-RADIAN * angle) * radius};
  };
  var getMaxRadius = exports.getMaxRadius = function(width, height) {
    var offset = 2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : {top:0, right:0, bottom:0, left:0};
    return Math.min(Math.abs(width - (offset.left || 0) - (offset.right || 0)), Math.abs(height - (offset.top || 0) - (offset.bottom || 0))) / 2;
  };
  exports.formatAxisMap = function(props, axisMap, offset, axisType, chartName) {
    var width = props.width, height = props.height, startAngle = props.startAngle, endAngle = props.endAngle, cx = (0,_DataUtils.getPercentValue)(props.cx, width, width / 2), cy = (0,_DataUtils.getPercentValue)(props.cy, height, height / 2);
    offset = getMaxRadius(width, height, offset);
    var innerRadius = (0,_DataUtils.getPercentValue)(props.innerRadius, offset, 0), outerRadius = (0,_DataUtils.getPercentValue)(props.outerRadius, offset, 0.8 * offset);
    return Object.keys(axisMap).reduce(function(result, id) {
      var axis = axisMap[id], domain = axis.domain, reversed = axis.reversed, range;
      if ((0,_isNil["default"])(axis.range)) {
        "angleAxis" === axisType ? range = [startAngle, endAngle] : "radiusAxis" === axisType && (range = [innerRadius, outerRadius]), reversed && (range = [range[1], range[0]]);
      } else {
        reversed = range = axis.range;
        var JSCompiler_temp;
        var JSCompiler_temp$jscomp$0 = Array.isArray(reversed) ? reversed : void 0;
        if (!JSCompiler_temp$jscomp$0) {
          a: {
            var t = null == reversed ? null : "undefined" != typeof Symbol && reversed[Symbol.iterator] || reversed["@@iterator"];
            if (null != t) {
              var i, u, a = [], f = !0, o = !1;
              try {
                for (i = (t = t.call(reversed)).next; !(f = (JSCompiler_temp = i.call(t)).done) && (a.push(JSCompiler_temp.value), 2 !== a.length); f = !0) {
                }
              } catch (r) {
                o = !0;
                var n = r;
              } finally {
                try {
                  if (!f && null != t["return"] && (u = t["return"](), Object(u) !== u)) {
                    JSCompiler_temp$jscomp$0 = void 0;
                    break a;
                  }
                } finally {
                  if (o) {
                    throw n;
                  }
                }
              }
              JSCompiler_temp$jscomp$0 = a;
            } else {
              JSCompiler_temp$jscomp$0 = void 0;
            }
          }
        }
        if (!(JSCompiler_temp = JSCompiler_temp$jscomp$0)) {
          a: {
            if (reversed) {
              if ("string" === typeof reversed) {
                JSCompiler_temp = _arrayLikeToArray(reversed, 2);
                break a;
              }
              JSCompiler_temp = Object.prototype.toString.call(reversed).slice(8, -1);
              "Object" === JSCompiler_temp && reversed.constructor && (JSCompiler_temp = reversed.constructor.name);
              if ("Map" === JSCompiler_temp || "Set" === JSCompiler_temp) {
                JSCompiler_temp = Array.from(reversed);
                break a;
              }
              if ("Arguments" === JSCompiler_temp || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(JSCompiler_temp)) {
                JSCompiler_temp = _arrayLikeToArray(reversed, 2);
                break a;
              }
            }
            JSCompiler_temp = void 0;
          }
        }
        if (!(reversed = JSCompiler_temp)) {
          throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
        }
        startAngle = reversed[0];
        endAngle = reversed[1];
      }
      JSCompiler_temp = (0,_ChartUtils.parseScale)(axis, chartName);
      reversed = JSCompiler_temp.realScaleType;
      JSCompiler_temp = JSCompiler_temp.scale;
      JSCompiler_temp.domain(domain).range(range);
      (0,_ChartUtils.checkDomainOfScale)(JSCompiler_temp);
      domain = (0,_ChartUtils.getTicksOfScale)(JSCompiler_temp, _objectSpread(_objectSpread({}, axis), {}, {realScaleType:reversed}));
      axis = _objectSpread(_objectSpread(_objectSpread({}, axis), domain), {}, {range, radius:outerRadius, realScaleType:reversed, scale:JSCompiler_temp, cx, cy, innerRadius, outerRadius, startAngle, endAngle});
      return _objectSpread(_objectSpread({}, result), {}, _defineProperty({}, id, axis));
    }, {});
  };
  var distanceBetweenPoints = exports.distanceBetweenPoints = function(point, anotherPoint) {
    return Math.sqrt(Math.pow(point.x - anotherPoint.x, 2) + Math.pow(point.y - anotherPoint.y, 2));
  }, getAngleOfPoint = exports.getAngleOfPoint = function(_ref, _ref2) {
    var x = _ref.x;
    _ref = _ref.y;
    var cx = _ref2.cx;
    _ref2 = _ref2.cy;
    var radius = distanceBetweenPoints({x, y:_ref}, {x:cx, y:_ref2});
    if (0 >= radius) {
      return {radius};
    }
    x = Math.acos((x - cx) / radius);
    _ref > _ref2 && (x = 2 * Math.PI - x);
    return {radius, angle:radianToDegree(x), angleInRadian:x};
  }, formatAngleOfSector = exports.formatAngleOfSector = function(_ref3) {
    var startAngle = _ref3.startAngle;
    _ref3 = _ref3.endAngle;
    var min = Math.min(Math.floor(startAngle / 360), Math.floor(_ref3 / 360));
    return {startAngle:startAngle - 360 * min, endAngle:_ref3 - 360 * min};
  };
  exports.inRangeOfSector = function(_ref5, sector) {
    var _getAngleOfPoint = getAngleOfPoint({x:_ref5.x, y:_ref5.y}, sector);
    _ref5 = _getAngleOfPoint.radius;
    _getAngleOfPoint = _getAngleOfPoint.angle;
    var outerRadius = sector.outerRadius;
    if (_ref5 < sector.innerRadius || _ref5 > outerRadius) {
      return !1;
    }
    if (0 === _ref5) {
      return !0;
    }
    var _formatAngleOfSector = formatAngleOfSector(sector);
    outerRadius = _formatAngleOfSector.startAngle;
    _formatAngleOfSector = _formatAngleOfSector.endAngle;
    if (outerRadius <= _formatAngleOfSector) {
      for (; _getAngleOfPoint > _formatAngleOfSector;) {
        _getAngleOfPoint -= 360;
      }
      for (; _getAngleOfPoint < outerRadius;) {
        _getAngleOfPoint += 360;
      }
      outerRadius = _getAngleOfPoint >= outerRadius && _getAngleOfPoint <= _formatAngleOfSector;
    } else {
      for (; _getAngleOfPoint > outerRadius;) {
        _getAngleOfPoint -= 360;
      }
      for (; _getAngleOfPoint < _formatAngleOfSector;) {
        _getAngleOfPoint += 360;
      }
      outerRadius = _getAngleOfPoint >= _formatAngleOfSector && _getAngleOfPoint <= outerRadius;
    }
    return outerRadius ? (outerRadius = _objectSpread({}, sector), _objectSpread(outerRadius, {}, {radius:_ref5, angle:_getAngleOfPoint + 360 * Math.min(Math.floor(sector.startAngle / 360), Math.floor(sector.endAngle / 360))})) : null;
  };
  exports.getTickClassName = function(tick) {
    return (0,_react.isValidElement)(tick) || (0,_isFunction["default"])(tick) || "boolean" === typeof tick ? "" : tick.className;
  };
};

//# sourceMappingURL=module$node_modules$recharts$lib$util$PolarUtils.js.map
