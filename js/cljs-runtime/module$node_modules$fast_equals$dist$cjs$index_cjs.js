shadow$provide.module$node_modules$fast_equals$dist$cjs$index_cjs = function(global, require, module, exports) {
  function combineComparators(comparatorA, comparatorB) {
    return function(a, b, state) {
      return comparatorA(a, b, state) && comparatorB(a, b, state);
    };
  }
  function createIsCircular(areItemsEqual) {
    return function(a, b, state) {
      if (!a || !b || "object" !== typeof a || "object" !== typeof b) {
        return areItemsEqual(a, b, state);
      }
      var cache = state.cache, cachedA = cache.get(a), cachedB = cache.get(b);
      if (cachedA && cachedB) {
        return cachedA === b && cachedB === a;
      }
      cache.set(a, b);
      cache.set(b, a);
      state = areItemsEqual(a, b, state);
      cache.delete(a);
      cache.delete(b);
      return state;
    };
  }
  function getStrictProperties(object) {
    return getOwnPropertyNames(object).concat(getOwnPropertySymbols(object));
  }
  function sameValueZeroEqual(a, b) {
    return a === b || !a && !b && a !== a && b !== b;
  }
  function areArraysEqual(a, b, state) {
    var index = a.length;
    if (b.length !== index) {
      return !1;
    }
    for (; 0 < index--;) {
      if (!state.equals(a[index], b[index], index, index, a, b, state)) {
        return !1;
      }
    }
    return !0;
  }
  function areDatesEqual(a, b) {
    return sameValueZeroEqual(a.getTime(), b.getTime());
  }
  function areErrorsEqual(a, b) {
    return a.name === b.name && a.message === b.message && a.cause === b.cause && a.stack === b.stack;
  }
  function areFunctionsEqual(a, b) {
    return a === b;
  }
  function areMapsEqual(a, b, state) {
    var size = a.size;
    if (size !== b.size) {
      return !1;
    }
    if (!size) {
      return !0;
    }
    size = Array(size);
    for (var aIterable = a.entries(), aResult, bResult, index = 0; (aResult = aIterable.next()) && !aResult.done;) {
      for (var bIterable = b.entries(), hasMatch = !1, matchIndex = 0; (bResult = bIterable.next()) && !bResult.done;) {
        if (!size[matchIndex]) {
          var aEntry = aResult.value;
          bResult = bResult.value;
          if (state.equals(aEntry[0], bResult[0], index, matchIndex, a, b, state) && state.equals(aEntry[1], bResult[1], aEntry[0], bResult[0], a, b, state)) {
            hasMatch = size[matchIndex] = !0;
            break;
          }
        }
        matchIndex++;
      }
      if (!hasMatch) {
        return !1;
      }
      index++;
    }
    return !0;
  }
  function areObjectsEqual(a, b, state) {
    var properties = keys(a), index = properties.length;
    if (keys(b).length !== index) {
      return !1;
    }
    for (; 0 < index--;) {
      if (!isPropertyEqual(a, b, state, properties[index])) {
        return !1;
      }
    }
    return !0;
  }
  function areObjectsEqualStrict(a, b, state) {
    var properties = getStrictProperties(a), index = properties.length;
    if (getStrictProperties(b).length !== index) {
      return !1;
    }
    for (var property, descriptorA; 0 < index--;) {
      property = properties[index];
      if (!isPropertyEqual(a, b, state, property)) {
        return !1;
      }
      descriptorA = getOwnPropertyDescriptor(a, property);
      property = getOwnPropertyDescriptor(b, property);
      if (!(!descriptorA && !property || descriptorA && property && descriptorA.configurable === property.configurable && descriptorA.enumerable === property.enumerable && descriptorA.writable === property.writable)) {
        return !1;
      }
    }
    return !0;
  }
  function arePrimitiveWrappersEqual(a, b) {
    return sameValueZeroEqual(a.valueOf(), b.valueOf());
  }
  function areRegExpsEqual(a, b) {
    return a.source === b.source && a.flags === b.flags;
  }
  function areSetsEqual(a, b, state) {
    var size = a.size;
    if (size !== b.size) {
      return !1;
    }
    if (!size) {
      return !0;
    }
    size = Array(size);
    for (var aIterable = a.values(), aResult, bResult; (aResult = aIterable.next()) && !aResult.done;) {
      for (var bIterable = b.values(), hasMatch = !1, matchIndex = 0; (bResult = bIterable.next()) && !bResult.done;) {
        if (!size[matchIndex] && state.equals(aResult.value, bResult.value, aResult.value, bResult.value, a, b, state)) {
          hasMatch = size[matchIndex] = !0;
          break;
        }
        matchIndex++;
      }
      if (!hasMatch) {
        return !1;
      }
    }
    return !0;
  }
  function areTypedArraysEqual(a, b) {
    var index = a.length;
    if (b.length !== index) {
      return !1;
    }
    for (; 0 < index--;) {
      if (a[index] !== b[index]) {
        return !1;
      }
    }
    return !0;
  }
  function areUrlsEqual(a, b) {
    return a.hostname === b.hostname && a.pathname === b.pathname && a.protocol === b.protocol && a.port === b.port && a.hash === b.hash && a.username === b.username && a.password === b.password;
  }
  function isPropertyEqual(a, b, state, property) {
    return "_owner" !== property && "__o" !== property && "__v" !== property || !a.$$typeof && !b.$$typeof ? hasOwn(b, property) && state.equals(a[property], b[property], property, property, a, b, state) : !0;
  }
  function createEqualityComparator(_a) {
    var areArraysEqual = _a.areArraysEqual, areDatesEqual = _a.areDatesEqual, areErrorsEqual = _a.areErrorsEqual, areFunctionsEqual = _a.areFunctionsEqual, areMapsEqual = _a.areMapsEqual, areNumbersEqual = _a.areNumbersEqual, areObjectsEqual = _a.areObjectsEqual, arePrimitiveWrappersEqual = _a.arePrimitiveWrappersEqual, areRegExpsEqual = _a.areRegExpsEqual, areSetsEqual = _a.areSetsEqual, areTypedArraysEqual = _a.areTypedArraysEqual, areUrlsEqual = _a.areUrlsEqual;
    return function(a, b, state) {
      if (a === b) {
        return !0;
      }
      if (null == a || null == b) {
        return !1;
      }
      var type = typeof a;
      if (type !== typeof b) {
        return !1;
      }
      if ("object" !== type) {
        return "number" === type ? areNumbersEqual(a, b, state) : "function" === type ? areFunctionsEqual(a, b, state) : !1;
      }
      type = a.constructor;
      if (type !== b.constructor) {
        return !1;
      }
      if (type === Object) {
        return areObjectsEqual(a, b, state);
      }
      if (isArray(a)) {
        return areArraysEqual(a, b, state);
      }
      if (null != isTypedArray && isTypedArray(a)) {
        return areTypedArraysEqual(a, b, state);
      }
      if (type === Date) {
        return areDatesEqual(a, b, state);
      }
      if (type === RegExp) {
        return areRegExpsEqual(a, b, state);
      }
      if (type === Map) {
        return areMapsEqual(a, b, state);
      }
      if (type === Set) {
        return areSetsEqual(a, b, state);
      }
      type = getTag(a);
      return "[object Date]" === type ? areDatesEqual(a, b, state) : "[object RegExp]" === type ? areRegExpsEqual(a, b, state) : "[object Map]" === type ? areMapsEqual(a, b, state) : "[object Set]" === type ? areSetsEqual(a, b, state) : "[object Object]" === type ? "function" !== typeof a.then && "function" !== typeof b.then && areObjectsEqual(a, b, state) : "[object URL]" === type ? areUrlsEqual(a, b, state) : "[object Error]" === type ? areErrorsEqual(a, b, state) : "[object Arguments]" === type ? 
      areObjectsEqual(a, b, state) : "[object Boolean]" === type || "[object Number]" === type || "[object String]" === type ? arePrimitiveWrappersEqual(a, b, state) : !1;
    };
  }
  function createInternalEqualityComparator(compare) {
    return function(a, b, _indexOrKeyA, _indexOrKeyB, _parentA, _parentB, state) {
      return compare(a, b, state);
    };
  }
  function createIsEqual(_a) {
    var circular = _a.circular, comparator = _a.comparator, createState = _a.createState, equals = _a.equals, strict = _a.strict;
    if (createState) {
      return function(a, b) {
        var _a = createState(), _b = _a.cache;
        _b = void 0 === _b ? circular ? new WeakMap() : void 0 : _b;
        return comparator(a, b, {cache:_b, equals, meta:_a.meta, strict});
      };
    }
    if (circular) {
      return function(a, b) {
        return comparator(a, b, {cache:new WeakMap(), equals, meta:void 0, strict});
      };
    }
    var state = {cache:void 0, equals, meta:void 0, strict};
    return function(a, b) {
      return comparator(a, b, state);
    };
  }
  function createCustomEqual(options) {
    void 0 === options && (options = {});
    var _a = options.circular;
    _a = void 0 === _a ? !1 : _a;
    var createCustomInternalComparator = options.createInternalComparator, createState = options.createState, _b = options.strict;
    _b = void 0 === _b ? !1 : _b;
    var circular = options.circular, createCustomConfig = options.createCustomConfig;
    options = options.strict;
    options = {areArraysEqual:options ? areObjectsEqualStrict : areArraysEqual, areDatesEqual, areErrorsEqual, areFunctionsEqual, areMapsEqual:options ? combineComparators(areMapsEqual, areObjectsEqualStrict) : areMapsEqual, areNumbersEqual, areObjectsEqual:options ? areObjectsEqualStrict : areObjectsEqual, arePrimitiveWrappersEqual, areRegExpsEqual, areSetsEqual:options ? combineComparators(areSetsEqual, areObjectsEqualStrict) : areSetsEqual, areTypedArraysEqual:options ? areObjectsEqualStrict : 
    areTypedArraysEqual, areUrlsEqual};
    createCustomConfig && (options = assign({}, options, createCustomConfig(options)));
    if (circular) {
      circular = createIsCircular(options.areArraysEqual);
      createCustomConfig = createIsCircular(options.areMapsEqual);
      var areObjectsEqual$1 = createIsCircular(options.areObjectsEqual), areSetsEqual$1 = createIsCircular(options.areSetsEqual);
      options = assign({}, options, {areArraysEqual:circular, areMapsEqual:createCustomConfig, areObjectsEqual:areObjectsEqual$1, areSetsEqual:areSetsEqual$1});
    }
    options = createEqualityComparator(options);
    createCustomInternalComparator = createCustomInternalComparator ? createCustomInternalComparator(options) : createInternalEqualityComparator(options);
    return createIsEqual({circular:_a, comparator:options, createState, equals:createCustomInternalComparator, strict:_b});
  }
  var getOwnPropertyNames = Object.getOwnPropertyNames, getOwnPropertySymbols = Object.getOwnPropertySymbols, hasOwnProperty = Object.prototype.hasOwnProperty, hasOwn = Object.hasOwn || function(object, property) {
    return hasOwnProperty.call(object, property);
  }, getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor, keys = Object.keys, areNumbersEqual = sameValueZeroEqual, isArray = Array.isArray, isTypedArray = "function" === typeof ArrayBuffer && ArrayBuffer.isView ? ArrayBuffer.isView : null, assign = Object.assign, getTag = Object.prototype.toString.call.bind(Object.prototype.toString);
  global = createCustomEqual();
  require = createCustomEqual({strict:!0});
  module = createCustomEqual({circular:!0});
  var strictCircularDeepEqual = createCustomEqual({circular:!0, strict:!0}), shallowEqual = createCustomEqual({createInternalComparator:function() {
    return sameValueZeroEqual;
  }}), strictShallowEqual = createCustomEqual({strict:!0, createInternalComparator:function() {
    return sameValueZeroEqual;
  }}), circularShallowEqual = createCustomEqual({circular:!0, createInternalComparator:function() {
    return sameValueZeroEqual;
  }}), strictCircularShallowEqual = createCustomEqual({circular:!0, createInternalComparator:function() {
    return sameValueZeroEqual;
  }, strict:!0});
  exports.circularDeepEqual = module;
  exports.circularShallowEqual = circularShallowEqual;
  exports.createCustomEqual = createCustomEqual;
  exports.deepEqual = global;
  exports.sameValueZeroEqual = sameValueZeroEqual;
  exports.shallowEqual = shallowEqual;
  exports.strictCircularDeepEqual = strictCircularDeepEqual;
  exports.strictCircularShallowEqual = strictCircularShallowEqual;
  exports.strictDeepEqual = require;
  exports.strictShallowEqual = strictShallowEqual;
};

//# sourceMappingURL=module$node_modules$fast_equals$dist$cjs$index_cjs.js.map
