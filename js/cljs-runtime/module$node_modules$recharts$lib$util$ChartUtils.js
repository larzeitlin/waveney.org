shadow$provide.module$node_modules$recharts$lib$util$ChartUtils = function(global, require, module, exports) {
  function _interopRequireDefault(obj) {
    return obj && obj.__esModule ? obj : {"default":obj};
  }
  function _getRequireWildcardCache(e) {
    if ("function" != typeof WeakMap) {
      return null;
    }
    var r = new WeakMap(), t = new WeakMap();
    return (_getRequireWildcardCache = function(e) {
      return e ? t : r;
    })(e);
  }
  function _typeof(o) {
    "@babel/helpers - typeof";
    return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o) {
      return typeof o;
    } : function(o) {
      return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
    }, _typeof(o);
  }
  function _toConsumableArray(arr) {
    var JSCompiler_temp = Array.isArray(arr) ? _arrayLikeToArray(arr) : void 0;
    JSCompiler_temp || (JSCompiler_temp = "undefined" !== typeof Symbol && null != arr[Symbol.iterator] || null != arr["@@iterator"] ? Array.from(arr) : void 0);
    if (!JSCompiler_temp) {
      a: {
        if (arr) {
          if ("string" === typeof arr) {
            JSCompiler_temp = _arrayLikeToArray(arr, void 0);
            break a;
          }
          JSCompiler_temp = Object.prototype.toString.call(arr).slice(8, -1);
          "Object" === JSCompiler_temp && arr.constructor && (JSCompiler_temp = arr.constructor.name);
          if ("Map" === JSCompiler_temp || "Set" === JSCompiler_temp) {
            JSCompiler_temp = Array.from(arr);
            break a;
          }
          if ("Arguments" === JSCompiler_temp || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(JSCompiler_temp)) {
            JSCompiler_temp = _arrayLikeToArray(arr, void 0);
            break a;
          }
        }
        JSCompiler_temp = void 0;
      }
    }
    if (!(arr = JSCompiler_temp)) {
      throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
    }
    return arr;
  }
  function _arrayLikeToArray(arr, len) {
    if (null == len || len > arr.length) {
      len = arr.length;
    }
    for (var i = 0, arr2 = Array(len); i < len; i++) {
      arr2[i] = arr[i];
    }
    return arr2;
  }
  function ownKeys(e, r) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r && (o = o.filter(function(r) {
        return Object.getOwnPropertyDescriptor(e, r).enumerable;
      }));
      t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread(e) {
    for (var r = 1; r < arguments.length; r++) {
      var t = null != arguments[r] ? arguments[r] : {};
      r % 2 ? ownKeys(Object(t), !0).forEach(function(r) {
        _defineProperty(e, r, t[r]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function(r) {
        Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
      });
    }
    return e;
  }
  function _defineProperty(obj, key, value) {
    a: {
      if ("object" == _typeof(key) && key) {
        var e = key[Symbol.toPrimitive];
        if (void 0 !== e) {
          key = e.call(key, "string");
          if ("object" != _typeof(key)) {
            break a;
          }
          throw new TypeError("@@toPrimitive must return a primitive value.");
        }
        key = String(key);
      }
    }
    key = "symbol" == _typeof(key) ? key : key + "";
    key in obj ? Object.defineProperty(obj, key, {value, enumerable:!0, configurable:!0, writable:!0}) : obj[key] = value;
    return obj;
  }
  function getValueByDataKey(obj, dataKey, defaultValue) {
    return (0,_isNil["default"])(obj) || (0,_isNil["default"])(dataKey) ? defaultValue : (0,_DataUtils.isNumOrStr)(dataKey) ? (0,_get["default"])(obj, dataKey, defaultValue) : (0,_isFunction["default"])(dataKey) ? dataKey(obj) : defaultValue;
  }
  function getDomainOfDataByKey(data, key, type, filterNil) {
    data = (0,_flatMap["default"])(data, function(entry) {
      return getValueByDataKey(entry, key);
    });
    return "number" === type ? (type = data.filter(function(entry) {
      return (0,_DataUtils.isNumber)(entry) || parseFloat(entry);
    }), type.length ? [(0,_min["default"])(type), (0,_max["default"])(type)] : [Infinity, -Infinity]) : (filterNil ? data.filter(function(entry) {
      return !(0,_isNil["default"])(entry);
    }) : data).map(function(entry) {
      return (0,_DataUtils.isNumOrStr)(entry) || entry instanceof Date ? entry : "";
    });
  }
  Object.defineProperty(exports, "__esModule", {value:!0});
  exports.getCateCoordinateOfBar = exports.getBaseValueOfBar = exports.getBarSizeList = exports.getBarPosition = exports.getBandSizeOfAxis = exports.findPositionOfBar = exports.combineEventHandlers = exports.checkDomainOfScale = exports.calculateActiveTickIndex = exports.appendOffsetOfLegend = exports.MIN_VALUE_REG = exports.MAX_VALUE_REG = void 0;
  exports.getCateCoordinateOfLine = function(_ref5) {
    var axis = _ref5.axis, ticks = _ref5.ticks, bandSize = _ref5.bandSize, entry = _ref5.entry, index = _ref5.index;
    _ref5 = _ref5.dataKey;
    if ("category" === axis.type) {
      return !axis.allowDuplicatedCategory && axis.dataKey && !(0,_isNil["default"])(entry[axis.dataKey]) && (axis = (0,_DataUtils.findEntryInArray)(ticks, "value", entry[axis.dataKey])) ? axis.coordinate + bandSize / 2 : ticks[index] ? ticks[index].coordinate + bandSize / 2 : null;
    }
    ticks = getValueByDataKey(entry, (0,_isNil["default"])(_ref5) ? axis.dataKey : _ref5);
    return (0,_isNil["default"])(ticks) ? null : axis.scale(ticks);
  };
  exports.getCoordinatesOfGrid = void 0;
  exports.getDomainOfDataByKey = getDomainOfDataByKey;
  exports.getDomainOfStackGroups = exports.getDomainOfItemsWithSameAxis = exports.getDomainOfErrorBars = void 0;
  Object.defineProperty(exports, "getLegendProps", {enumerable:!0, get:function() {
    return _getLegendProps.getLegendProps;
  }});
  exports.getTooltipItem = exports.getTicksOfScale = exports.getTicksOfAxis = exports.getStackedDataOfItem = exports.getStackedData = exports.getStackGroupsByAxisId = exports.getMainColorOfGraphicItem = void 0;
  exports.getValueByDataKey = getValueByDataKey;
  exports.truncateByDomain = exports.parseSpecifiedDomain = exports.parseScale = exports.parseErrorBarsOfAxis = exports.parseDomainOfCategoryAxis = exports.offsetSign = exports.offsetPositive = exports.isCategoricalAxis = void 0;
  var d3Scales = function(e, r) {
    if (!r && e && e.__esModule) {
      return e;
    }
    if (null === e || "object" != _typeof(e) && "function" != typeof e) {
      return {"default":e};
    }
    if ((r = _getRequireWildcardCache(r)) && r.has(e)) {
      return r.get(e);
    }
    var n = {__proto__:null}, a = Object.defineProperty && Object.getOwnPropertyDescriptor, u;
    for (u in e) {
      if ("default" !== u && {}.hasOwnProperty.call(e, u)) {
        var i = a ? Object.getOwnPropertyDescriptor(e, u) : null;
        i && (i.get || i.set) ? Object.defineProperty(n, u, i) : n[u] = e[u];
      }
    }
    return n["default"] = e, r && r.set(e, n), n;
  }(require("module$node_modules$victory_vendor$lib$d3_scale")), _d3Shape = require("module$node_modules$victory_vendor$lib$d3_shape"), _max = _interopRequireDefault(require("module$node_modules$lodash$max")), _min = _interopRequireDefault(require("module$node_modules$lodash$min")), _isNil = _interopRequireDefault(require("module$node_modules$lodash$isNil")), _isFunction = _interopRequireDefault(require("module$node_modules$lodash$isFunction")), _isString = _interopRequireDefault(require("module$node_modules$lodash$isString")), 
  _get = _interopRequireDefault(require("module$node_modules$lodash$get")), _flatMap = _interopRequireDefault(require("module$node_modules$lodash$flatMap")), _isNaN = _interopRequireDefault(require("module$node_modules$lodash$isNaN")), _upperFirst = _interopRequireDefault(require("module$node_modules$lodash$upperFirst")), _isEqual = _interopRequireDefault(require("module$node_modules$lodash$isEqual")), _sortBy = _interopRequireDefault(require("module$node_modules$lodash$sortBy")), _rechartsScale = 
  require("module$node_modules$recharts_scale$lib$index"), _ErrorBar = require("module$node_modules$recharts$lib$cartesian$ErrorBar"), _DataUtils = require("module$node_modules$recharts$lib$util$DataUtils"), _ReactUtils = require("module$node_modules$recharts$lib$util$ReactUtils"), _getLegendProps = require("module$node_modules$recharts$lib$util$getLegendProps");
  exports.calculateActiveTickIndex = function(coordinate) {
    var _ticks$length, ticks = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : [], unsortedTicks = 2 < arguments.length ? arguments[2] : void 0, axis = 3 < arguments.length ? arguments[3] : void 0, index = -1, len = null !== (_ticks$length = null === ticks || void 0 === ticks ? void 0 : ticks.length) && void 0 !== _ticks$length ? _ticks$length : 0;
    if (1 >= len) {
      return 0;
    }
    if (axis && "angleAxis" === axis.axisType && 1e-6 >= Math.abs(Math.abs(axis.range[1] - axis.range[0]) - 360)) {
      for (ticks = axis.range, _ticks$length = 0; _ticks$length < len; _ticks$length++) {
        var before = 0 < _ticks$length ? unsortedTicks[_ticks$length - 1].coordinate : unsortedTicks[len - 1].coordinate;
        axis = unsortedTicks[_ticks$length].coordinate;
        var after = _ticks$length >= len - 1 ? unsortedTicks[0].coordinate : unsortedTicks[_ticks$length + 1].coordinate;
        if ((0,_DataUtils.mathSign)(axis - before) !== (0,_DataUtils.mathSign)(after - axis)) {
          var diffInterval = [];
          if ((0,_DataUtils.mathSign)(after - axis) === (0,_DataUtils.mathSign)(ticks[1] - ticks[0])) {
            var sameDirectionCoord = after;
            after = axis + ticks[1] - ticks[0];
            diffInterval[0] = Math.min(after, (after + before) / 2);
            diffInterval[1] = Math.max(after, (after + before) / 2);
          } else {
            sameDirectionCoord = before, before = after + ticks[1] - ticks[0], diffInterval[0] = Math.min(axis, (before + axis) / 2), diffInterval[1] = Math.max(axis, (before + axis) / 2);
          }
          axis = [Math.min(axis, (sameDirectionCoord + axis) / 2), Math.max(axis, (sameDirectionCoord + axis) / 2)];
          if (coordinate > axis[0] && coordinate <= axis[1] || coordinate >= diffInterval[0] && coordinate <= diffInterval[1]) {
            index = unsortedTicks[_ticks$length].index;
            break;
          }
        } else {
          if (diffInterval = Math.max(before, after), coordinate > (Math.min(before, after) + axis) / 2 && coordinate <= (diffInterval + axis) / 2) {
            index = unsortedTicks[_ticks$length].index;
            break;
          }
        }
      }
    } else {
      for (unsortedTicks = 0; unsortedTicks < len; unsortedTicks++) {
        if (0 === unsortedTicks && coordinate <= (ticks[unsortedTicks].coordinate + ticks[unsortedTicks + 1].coordinate) / 2 || 0 < unsortedTicks && unsortedTicks < len - 1 && coordinate > (ticks[unsortedTicks].coordinate + ticks[unsortedTicks - 1].coordinate) / 2 && coordinate <= (ticks[unsortedTicks].coordinate + ticks[unsortedTicks + 1].coordinate) / 2 || unsortedTicks === len - 1 && coordinate > (ticks[unsortedTicks].coordinate + ticks[unsortedTicks - 1].coordinate) / 2) {
          index = ticks[unsortedTicks].index;
          break;
        }
      }
    }
    return index;
  };
  var getMainColorOfGraphicItem = exports.getMainColorOfGraphicItem = function(item) {
    var _item$type, displayName = item.type.displayName, defaultedProps = null !== (_item$type = item.type) && void 0 !== _item$type && _item$type.defaultProps ? _objectSpread(_objectSpread({}, item.type.defaultProps), item.props) : item.props;
    item = defaultedProps.stroke;
    defaultedProps = defaultedProps.fill;
    switch(displayName) {
      case "Line":
        displayName = item;
        break;
      case "Area":
      case "Radar":
        displayName = item && "none" !== item ? item : defaultedProps;
        break;
      default:
        displayName = defaultedProps;
    }
    return displayName;
  };
  exports.getBarSizeList = function(_ref2) {
    var globalSize = _ref2.barSize, totalSize = _ref2.totalSize;
    _ref2 = _ref2.stackGroups;
    _ref2 = void 0 === _ref2 ? {} : _ref2;
    if (!_ref2) {
      return {};
    }
    for (var result = {}, numericAxisIds = Object.keys(_ref2), i = 0, len = numericAxisIds.length; i < len; i++) {
      for (var sgs = _ref2[numericAxisIds[i]].stackGroups, stackIds = Object.keys(sgs), j = 0, sLen = stackIds.length; j < sLen; j++) {
        var _sgs$stackIds$j = sgs[stackIds[j]], cateAxisId = _sgs$stackIds$j.cateAxisId;
        if ((_sgs$stackIds$j = _sgs$stackIds$j.items.filter(function(item) {
          return 0 <= (0,_ReactUtils.getDisplayName)(item.type).indexOf("Bar");
        })) && _sgs$stackIds$j.length) {
          var barItemDefaultProps = _sgs$stackIds$j[0].type.defaultProps, barItemProps = void 0 !== barItemDefaultProps ? _objectSpread(_objectSpread({}, barItemDefaultProps), _sgs$stackIds$j[0].props) : _sgs$stackIds$j[0].props;
          barItemDefaultProps = barItemProps.barSize;
          cateAxisId = barItemProps[cateAxisId];
          result[cateAxisId] || (result[cateAxisId] = []);
          barItemDefaultProps = (0,_isNil["default"])(barItemDefaultProps) ? globalSize : barItemDefaultProps;
          result[cateAxisId].push({item:_sgs$stackIds$j[0], stackList:_sgs$stackIds$j.slice(1), barSize:(0,_isNil["default"])(barItemDefaultProps) ? void 0 : (0,_DataUtils.getPercentValue)(barItemDefaultProps, totalSize, 0)});
        }
      }
    }
    return result;
  };
  exports.getBarPosition = function(_ref3) {
    var barCategoryGap = _ref3.barCategoryGap, bandSize = _ref3.bandSize, _ref3$sizeList = _ref3.sizeList;
    _ref3$sizeList = void 0 === _ref3$sizeList ? [] : _ref3$sizeList;
    var maxBarSize = _ref3.maxBarSize, len = _ref3$sizeList.length;
    if (1 > len) {
      return null;
    }
    var realBarGap = (0,_DataUtils.getPercentValue)(_ref3.barGap, bandSize, 0, !0);
    _ref3 = [];
    if (_ref3$sizeList[0].barSize === +_ref3$sizeList[0].barSize) {
      var useFull = !1, fullBarSize = bandSize / len;
      barCategoryGap = _ref3$sizeList.reduce(function(res, entry) {
        return res + entry.barSize || 0;
      }, 0);
      barCategoryGap += (len - 1) * realBarGap;
      barCategoryGap >= bandSize && (barCategoryGap -= (len - 1) * realBarGap, realBarGap = 0);
      barCategoryGap >= bandSize && 0 < fullBarSize && (useFull = !0, fullBarSize *= 0.9, barCategoryGap = len * fullBarSize);
      var prev = {offset:((bandSize - barCategoryGap) / 2 >> 0) - realBarGap, size:0};
      bandSize = _ref3$sizeList.reduce(function(res, entry) {
        var newPosition = {item:entry.item, position:{offset:prev.offset + prev.size + realBarGap, size:useFull ? fullBarSize : entry.barSize}}, newRes = [].concat(_toConsumableArray(res), [newPosition]);
        prev = newRes[newRes.length - 1].position;
        entry.stackList && entry.stackList.length && entry.stackList.forEach(function(item) {
          newRes.push({item, position:prev});
        });
        return newRes;
      }, _ref3);
    } else {
      var _offset = (0,_DataUtils.getPercentValue)(barCategoryGap, bandSize, 0, !0);
      0 >= bandSize - 2 * _offset - (len - 1) * realBarGap && (realBarGap = 0);
      var originalSize = (bandSize - 2 * _offset - (len - 1) * realBarGap) / len;
      1 < originalSize && (originalSize >>= 0);
      var size = maxBarSize === +maxBarSize ? Math.min(originalSize, maxBarSize) : originalSize;
      bandSize = _ref3$sizeList.reduce(function(res, entry, i) {
        var newRes = [].concat(_toConsumableArray(res), [{item:entry.item, position:{offset:_offset + (originalSize + realBarGap) * i + (originalSize - size) / 2, size}}]);
        entry.stackList && entry.stackList.length && entry.stackList.forEach(function(item) {
          newRes.push({item, position:newRes[newRes.length - 1].position});
        });
        return newRes;
      }, _ref3);
    }
    return bandSize;
  };
  exports.appendOffsetOfLegend = function(offset, _unused, props, legendBox) {
    _unused = props.margin;
    if (props = (0,_getLegendProps.getLegendProps)({children:props.children, legendWidth:props.width - (_unused.left || 0) - (_unused.right || 0)})) {
      _unused = legendBox || {};
      legendBox = _unused.width;
      _unused = _unused.height;
      var align = props.align, verticalAlign = props.verticalAlign;
      props = props.layout;
      if (("vertical" === props || "horizontal" === props && "middle" === verticalAlign) && "center" !== align && (0,_DataUtils.isNumber)(offset[align])) {
        return _objectSpread(_objectSpread({}, offset), {}, _defineProperty({}, align, offset[align] + (legendBox || 0)));
      }
      if (("horizontal" === props || "vertical" === props && "center" === align) && "middle" !== verticalAlign && (0,_DataUtils.isNumber)(offset[verticalAlign])) {
        return _objectSpread(_objectSpread({}, offset), {}, _defineProperty({}, verticalAlign, offset[verticalAlign] + (_unused || 0)));
      }
    }
    return offset;
  };
  var getDomainOfErrorBars = exports.getDomainOfErrorBars = function(data, item, dataKey, layout, axisType) {
    if ((item = (0,_ReactUtils.findAllByType)(item.props.children, _ErrorBar.ErrorBar).filter(function(errorBarChild) {
      errorBarChild = errorBarChild.props.direction;
      return (0,_isNil["default"])(axisType) ? !0 : "horizontal" === layout ? "yAxis" === axisType : "vertical" === layout || "x" === errorBarChild ? "xAxis" === axisType : "y" === errorBarChild ? "yAxis" === axisType : !0;
    })) && item.length) {
      var keys = item.map(function(errorBarChild) {
        return errorBarChild.props.dataKey;
      });
      return data.reduce(function(result, entry) {
        var entryValue = getValueByDataKey(entry, dataKey);
        if ((0,_isNil["default"])(entryValue)) {
          return result;
        }
        var mainValue = Array.isArray(entryValue) ? [(0,_min["default"])(entryValue), (0,_max["default"])(entryValue)] : [entryValue, entryValue];
        entryValue = keys.reduce(function(prevErrorArr, k) {
          var errorValue = getValueByDataKey(entry, k, 0);
          k = mainValue[0] - Math.abs(Array.isArray(errorValue) ? errorValue[0] : errorValue);
          errorValue = mainValue[1] + Math.abs(Array.isArray(errorValue) ? errorValue[1] : errorValue);
          return [Math.min(k, prevErrorArr[0]), Math.max(errorValue, prevErrorArr[1])];
        }, [Infinity, -Infinity]);
        return [Math.min(entryValue[0], result[0]), Math.max(entryValue[1], result[1])];
      }, [Infinity, -Infinity]);
    }
    return null;
  };
  exports.parseErrorBarsOfAxis = function(data, items, dataKey, axisType, layout) {
    return (items = items.map(function(item) {
      return getDomainOfErrorBars(data, item, dataKey, layout, axisType);
    }).filter(function(entry) {
      return !(0,_isNil["default"])(entry);
    })) && items.length ? items.reduce(function(result, entry) {
      return [Math.min(result[0], entry[0]), Math.max(result[1], entry[1])];
    }, [Infinity, -Infinity]) : null;
  };
  exports.getDomainOfItemsWithSameAxis = function(data, items, type, layout, filterNil) {
    items = items.map(function(item) {
      var dataKey = item.props.dataKey;
      return "number" === type && dataKey ? getDomainOfErrorBars(data, item, dataKey, layout) || getDomainOfDataByKey(data, dataKey, type, filterNil) : getDomainOfDataByKey(data, dataKey, type, filterNil);
    });
    if ("number" === type) {
      return items.reduce(function(result, entry) {
        return [Math.min(result[0], entry[0]), Math.max(result[1], entry[1])];
      }, [Infinity, -Infinity]);
    }
    var tag = {};
    return items.reduce(function(result, entry) {
      for (var i = 0, len = entry.length; i < len; i++) {
        tag[entry[i]] || (tag[entry[i]] = !0, result.push(entry[i]));
      }
      return result;
    }, []);
  };
  exports.isCategoricalAxis = function(layout, axisType) {
    return "horizontal" === layout && "xAxis" === axisType || "vertical" === layout && "yAxis" === axisType || "centric" === layout && "angleAxis" === axisType || "radial" === layout && "radiusAxis" === axisType;
  };
  exports.getCoordinatesOfGrid = function(ticks, minValue, maxValue, syncWithTicks) {
    if (syncWithTicks) {
      return ticks.map(function(entry) {
        return entry.coordinate;
      });
    }
    var hasMin, hasMax;
    ticks = ticks.map(function(entry) {
      entry.coordinate === minValue && (hasMin = !0);
      entry.coordinate === maxValue && (hasMax = !0);
      return entry.coordinate;
    });
    hasMin || ticks.push(minValue);
    hasMax || ticks.push(maxValue);
    return ticks;
  };
  exports.getTicksOfAxis = function(axis, isGrid, isAll) {
    if (!axis) {
      return null;
    }
    var scale = axis.scale, duplicateDomain = axis.duplicateDomain, type = axis.type, range = axis.range, offsetForBand = "scaleBand" === axis.realScaleType ? scale.bandwidth() / 2 : 2, offset = (isGrid || isAll) && "category" === type && scale.bandwidth ? scale.bandwidth() / offsetForBand : 0;
    offset = "angleAxis" === axis.axisType && 2 <= (null === range || void 0 === range ? void 0 : range.length) ? 2 * (0,_DataUtils.mathSign)(range[0] - range[1]) * offset : offset;
    return isGrid && (axis.ticks || axis.niceTicks) ? (axis.ticks || axis.niceTicks).map(function(entry) {
      var scaleContent = duplicateDomain ? duplicateDomain.indexOf(entry) : entry;
      return {coordinate:scale(scaleContent) + offset, value:entry, offset};
    }).filter(function(row) {
      return !(0,_isNaN["default"])(row.coordinate);
    }) : axis.isCategorical && axis.categoricalDomain ? axis.categoricalDomain.map(function(entry, index) {
      return {coordinate:scale(entry) + offset, value:entry, index, offset};
    }) : scale.ticks && !isAll ? scale.ticks(axis.tickCount).map(function(entry) {
      return {coordinate:scale(entry) + offset, value:entry, offset};
    }) : scale.domain().map(function(entry, index) {
      return {coordinate:scale(entry) + offset, value:duplicateDomain ? duplicateDomain[entry] : entry, index, offset};
    });
  };
  var handlerWeakMap = new WeakMap();
  exports.combineEventHandlers = function(defaultHandler, childHandler) {
    if ("function" !== typeof childHandler) {
      return defaultHandler;
    }
    handlerWeakMap.has(defaultHandler) || handlerWeakMap.set(defaultHandler, new WeakMap());
    var childWeakMap = handlerWeakMap.get(defaultHandler);
    if (childWeakMap.has(childHandler)) {
      return childWeakMap.get(childHandler);
    }
    var combineHandler = function() {
      defaultHandler.apply(void 0, arguments);
      childHandler.apply(void 0, arguments);
    };
    childWeakMap.set(childHandler, combineHandler);
    return combineHandler;
  };
  exports.parseScale = function(axis, chartType, hasBar) {
    var scale = axis.scale, type = axis.type, layout = axis.layout;
    axis = axis.axisType;
    return "auto" === scale ? "radial" === layout && "radiusAxis" === axis ? {scale:d3Scales.scaleBand(), realScaleType:"band"} : "radial" === layout && "angleAxis" === axis ? {scale:d3Scales.scaleLinear(), realScaleType:"linear"} : "category" === type && chartType && (0 <= chartType.indexOf("LineChart") || 0 <= chartType.indexOf("AreaChart") || 0 <= chartType.indexOf("ComposedChart") && !hasBar) ? {scale:d3Scales.scalePoint(), realScaleType:"point"} : "category" === type ? {scale:d3Scales.scaleBand(), 
    realScaleType:"band"} : {scale:d3Scales.scaleLinear(), realScaleType:"linear"} : (0,_isString["default"])(scale) ? (chartType = "scale".concat((0,_upperFirst["default"])(scale)), {scale:(d3Scales[chartType] || d3Scales.scalePoint)(), realScaleType:d3Scales[chartType] ? chartType : "point"}) : (0,_isFunction["default"])(scale) ? {scale} : {scale:d3Scales.scalePoint(), realScaleType:"point"};
  };
  exports.checkDomainOfScale = function(scale) {
    var domain = scale.domain();
    if (domain && !(2 >= domain.length)) {
      var len = domain.length, range = scale.range(), minValue = Math.min(range[0], range[1]) - 1e-4;
      range = Math.max(range[0], range[1]) + 1e-4;
      var first = scale(domain[0]), last = scale(domain[len - 1]);
      (first < minValue || first > range || last < minValue || last > range) && scale.domain([domain[0], domain[len - 1]]);
    }
  };
  exports.findPositionOfBar = function(barPosition, child) {
    if (!barPosition) {
      return null;
    }
    for (var i = 0, len = barPosition.length; i < len; i++) {
      if (barPosition[i].item === child) {
        return barPosition[i].position;
      }
    }
    return null;
  };
  exports.truncateByDomain = function(value, domain) {
    if (!domain || 2 !== domain.length || !(0,_DataUtils.isNumber)(domain[0]) || !(0,_DataUtils.isNumber)(domain[1])) {
      return value;
    }
    var minValue = Math.min(domain[0], domain[1]);
    domain = Math.max(domain[0], domain[1]);
    var result = [value[0], value[1]];
    if (!(0,_DataUtils.isNumber)(value[0]) || value[0] < minValue) {
      result[0] = minValue;
    }
    if (!(0,_DataUtils.isNumber)(value[1]) || value[1] > domain) {
      result[1] = domain;
    }
    result[0] > domain && (result[0] = domain);
    result[1] < minValue && (result[1] = minValue);
    return result;
  };
  global = exports.offsetSign = function(series) {
    var n = series.length;
    if (!(0 >= n)) {
      for (var j = 0, m = series[0].length; j < m; ++j) {
        for (var positive = 0, negative = 0, i = 0; i < n; ++i) {
          var value = (0,_isNaN["default"])(series[i][j][1]) ? series[i][j][0] : series[i][j][1];
          0 <= value ? (series[i][j][0] = positive, series[i][j][1] = positive + value, positive = series[i][j][1]) : (series[i][j][0] = negative, series[i][j][1] = negative + value, negative = series[i][j][1]);
        }
      }
    }
  };
  require = exports.offsetPositive = function(series) {
    var n = series.length;
    if (!(0 >= n)) {
      for (var j = 0, m = series[0].length; j < m; ++j) {
        for (var positive = 0, i = 0; i < n; ++i) {
          var value = (0,_isNaN["default"])(series[i][j][1]) ? series[i][j][0] : series[i][j][1];
          0 <= value ? (series[i][j][0] = positive, series[i][j][1] = positive + value, positive = series[i][j][1]) : (series[i][j][0] = 0, series[i][j][1] = 0);
        }
      }
    }
  };
  var STACK_OFFSET_MAP = {sign:global, expand:_d3Shape.stackOffsetExpand, none:_d3Shape.stackOffsetNone, silhouette:_d3Shape.stackOffsetSilhouette, wiggle:_d3Shape.stackOffsetWiggle, positive:require}, getStackedData = exports.getStackedData = function(data, stackItems, offsetType) {
    stackItems = stackItems.map(function(item) {
      return item.props.dataKey;
    });
    offsetType = STACK_OFFSET_MAP[offsetType];
    return (0,_d3Shape.stack)().keys(stackItems).value(function(d, key) {
      return +getValueByDataKey(d, key, 0);
    }).order(_d3Shape.stackOrderNone).offset(offsetType)(data);
  };
  exports.getStackGroupsByAxisId = function(data, _items, numericAxisId, cateAxisId, offsetType, reverseStackOrder) {
    if (!data) {
      return null;
    }
    var stackGroups = (reverseStackOrder ? _items.reverse() : _items).reduce(function(result, item) {
      var _item$type2, defaultedProps = null !== (_item$type2 = item.type) && void 0 !== _item$type2 && _item$type2.defaultProps ? _objectSpread(_objectSpread({}, item.type.defaultProps), item.props) : item.props;
      _item$type2 = defaultedProps.stackId;
      if (defaultedProps.hide) {
        return result;
      }
      defaultedProps = defaultedProps[numericAxisId];
      var parentGroup = result[defaultedProps] || {hasStack:!1, stackGroups:{}};
      if ((0,_DataUtils.isNumOrStr)(_item$type2)) {
        var childGroup = parentGroup.stackGroups[_item$type2] || {numericAxisId, cateAxisId, items:[]};
        childGroup.items.push(item);
        parentGroup.hasStack = !0;
        parentGroup.stackGroups[_item$type2] = childGroup;
      } else {
        parentGroup.stackGroups[(0,_DataUtils.uniqueId)("_stackId_")] = {numericAxisId, cateAxisId, items:[item]};
      }
      return _objectSpread(_objectSpread({}, result), {}, _defineProperty({}, defaultedProps, parentGroup));
    }, {});
    return Object.keys(stackGroups).reduce(function(result, axisId) {
      var group = stackGroups[axisId];
      group.hasStack && (group.stackGroups = Object.keys(group.stackGroups).reduce(function(res, stackId) {
        var g = group.stackGroups[stackId];
        return _objectSpread(_objectSpread({}, res), {}, _defineProperty({}, stackId, {numericAxisId, cateAxisId, items:g.items, stackedData:getStackedData(data, g.items, offsetType)}));
      }, {}));
      return _objectSpread(_objectSpread({}, result), {}, _defineProperty({}, axisId, group));
    }, {});
  };
  exports.getTicksOfScale = function(scale, opts) {
    var type = opts.type, tickCount = opts.tickCount, originalDomain = opts.originalDomain, allowDecimals = opts.allowDecimals;
    opts = opts.realScaleType || opts.scale;
    if ("auto" !== opts && "linear" !== opts) {
      return null;
    }
    if (tickCount && "number" === type && originalDomain && ("auto" === originalDomain[0] || "auto" === originalDomain[1])) {
      type = scale.domain();
      if (!type.length) {
        return null;
      }
      tickCount = (0,_rechartsScale.getNiceTickValues)(type, tickCount, allowDecimals);
      scale.domain([(0,_min["default"])(tickCount), (0,_max["default"])(tickCount)]);
      return {niceTicks:tickCount};
    }
    return tickCount && "number" === type ? (scale = scale.domain(), {niceTicks:(0,_rechartsScale.getTickValuesFixedDomain)(scale, tickCount, allowDecimals)}) : null;
  };
  exports.getCateCoordinateOfBar = function(_ref6) {
    var axis = _ref6.axis, ticks = _ref6.ticks, offset = _ref6.offset, bandSize = _ref6.bandSize, index = _ref6.index;
    if ("category" === axis.type) {
      return ticks[index] ? ticks[index].coordinate + offset : null;
    }
    _ref6 = getValueByDataKey(_ref6.entry, axis.dataKey, axis.domain[index]);
    return (0,_isNil["default"])(_ref6) ? null : axis.scale(_ref6) - bandSize / 2 + offset;
  };
  exports.getBaseValueOfBar = function(_ref7) {
    var numericAxis = _ref7.numericAxis;
    _ref7 = numericAxis.scale.domain();
    return "number" === numericAxis.type ? (numericAxis = Math.min(_ref7[0], _ref7[1]), _ref7 = Math.max(_ref7[0], _ref7[1]), 0 >= numericAxis && 0 <= _ref7 ? 0 : 0 > _ref7 ? _ref7 : numericAxis) : _ref7[0];
  };
  exports.getStackedDataOfItem = function(item, stackGroups) {
    var _item$type3, stackId = (null !== (_item$type3 = item.type) && void 0 !== _item$type3 && _item$type3.defaultProps ? _objectSpread(_objectSpread({}, item.type.defaultProps), item.props) : item.props).stackId;
    return (0,_DataUtils.isNumOrStr)(stackId) && (stackGroups = stackGroups[stackId]) ? (item = stackGroups.items.indexOf(item), 0 <= item ? stackGroups.stackedData[item] : null) : null;
  };
  var getDomainOfSingle = function(data) {
    return data.reduce(function(result, entry) {
      return [(0,_min["default"])(entry.concat([result[0]]).filter(_DataUtils.isNumber)), (0,_max["default"])(entry.concat([result[1]]).filter(_DataUtils.isNumber))];
    }, [Infinity, -Infinity]);
  };
  exports.getDomainOfStackGroups = function(stackGroups, startIndex, endIndex) {
    return Object.keys(stackGroups).reduce(function(result, stackId) {
      stackId = stackGroups[stackId].stackedData.reduce(function(res, entry) {
        entry = getDomainOfSingle(entry.slice(startIndex, endIndex + 1));
        return [Math.min(res[0], entry[0]), Math.max(res[1], entry[1])];
      }, [Infinity, -Infinity]);
      return [Math.min(stackId[0], result[0]), Math.max(stackId[1], result[1])];
    }, [Infinity, -Infinity]).map(function(result) {
      return Infinity === result || -Infinity === result ? 0 : result;
    });
  };
  var MIN_VALUE_REG = exports.MIN_VALUE_REG = /^dataMin[\s]*-[\s]*([0-9]+([.]{1}[0-9]+){0,1})$/, MAX_VALUE_REG = exports.MAX_VALUE_REG = /^dataMax[\s]*\+[\s]*([0-9]+([.]{1}[0-9]+){0,1})$/;
  exports.parseSpecifiedDomain = function(specifiedDomain, dataDomain, allowDataOverflow) {
    if ((0,_isFunction["default"])(specifiedDomain)) {
      return specifiedDomain(dataDomain, allowDataOverflow);
    }
    if (!Array.isArray(specifiedDomain)) {
      return dataDomain;
    }
    var domain = [];
    if ((0,_DataUtils.isNumber)(specifiedDomain[0])) {
      domain[0] = allowDataOverflow ? specifiedDomain[0] : Math.min(specifiedDomain[0], dataDomain[0]);
    } else if (MIN_VALUE_REG.test(specifiedDomain[0])) {
      var value = +MIN_VALUE_REG.exec(specifiedDomain[0])[1];
      domain[0] = dataDomain[0] - value;
    } else {
      (0,_isFunction["default"])(specifiedDomain[0]) ? domain[0] = specifiedDomain[0](dataDomain[0]) : domain[0] = dataDomain[0];
    }
    (0,_DataUtils.isNumber)(specifiedDomain[1]) ? domain[1] = allowDataOverflow ? specifiedDomain[1] : Math.max(specifiedDomain[1], dataDomain[1]) : MAX_VALUE_REG.test(specifiedDomain[1]) ? (specifiedDomain = +MAX_VALUE_REG.exec(specifiedDomain[1])[1], domain[1] = dataDomain[1] + specifiedDomain) : (0,_isFunction["default"])(specifiedDomain[1]) ? domain[1] = specifiedDomain[1](dataDomain[1]) : domain[1] = dataDomain[1];
    return domain;
  };
  exports.getBandSizeOfAxis = function(axis, ticks, isBar) {
    if (axis && axis.scale && axis.scale.bandwidth) {
      var bandWidth = axis.scale.bandwidth();
      if (!isBar || 0 < bandWidth) {
        return bandWidth;
      }
    }
    if (axis && ticks && 2 <= ticks.length) {
      axis = (0,_sortBy["default"])(ticks, function(o) {
        return o.coordinate;
      });
      ticks = Infinity;
      isBar = 1;
      for (bandWidth = axis.length; isBar < bandWidth; isBar++) {
        ticks = Math.min((axis[isBar].coordinate || 0) - (axis[isBar - 1].coordinate || 0), ticks);
      }
      return Infinity === ticks ? 0 : ticks;
    }
    return isBar ? void 0 : 0;
  };
  exports.parseDomainOfCategoryAxis = function(specifiedDomain, calculatedDomain, axisChild) {
    return specifiedDomain && specifiedDomain.length && !(0,_isEqual["default"])(specifiedDomain, (0,_get["default"])(axisChild, "type.defaultProps.domain")) ? specifiedDomain : calculatedDomain;
  };
  exports.getTooltipItem = function(graphicalItem, payload) {
    var defaultedProps = graphicalItem.type.defaultProps ? _objectSpread(_objectSpread({}, graphicalItem.type.defaultProps), graphicalItem.props) : graphicalItem.props, dataKey = defaultedProps.dataKey, name = defaultedProps.name, unit = defaultedProps.unit, formatter = defaultedProps.formatter, tooltipType = defaultedProps.tooltipType, chartType = defaultedProps.chartType;
    defaultedProps = defaultedProps.hide;
    return _objectSpread(_objectSpread({}, (0,_ReactUtils.filterProps)(graphicalItem, !1)), {}, {dataKey, unit, formatter, name:name || dataKey, color:getMainColorOfGraphicItem(graphicalItem), value:getValueByDataKey(payload, dataKey), type:tooltipType, payload, chartType, hide:defaultedProps});
  };
};

//# sourceMappingURL=module$node_modules$recharts$lib$util$ChartUtils.js.map
