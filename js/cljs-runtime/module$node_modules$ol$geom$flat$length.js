shadow$provide.module$node_modules$ol$geom$flat$length = function(global, require, module, exports) {
  function lineStringLength(flatCoordinates, offset, end, stride) {
    let x1 = flatCoordinates[offset], y1 = flatCoordinates[offset + 1], length = 0;
    for (offset += stride; offset < end; offset += stride) {
      const x2 = flatCoordinates[offset], y2 = flatCoordinates[offset + 1];
      length += Math.sqrt((x2 - x1) * (x2 - x1) + (y2 - y1) * (y2 - y1));
      x1 = x2;
      y1 = y2;
    }
    return length;
  }
  function linearRingLength(flatCoordinates, offset, end, stride) {
    let perimeter = lineStringLength(flatCoordinates, offset, end, stride);
    const dx = flatCoordinates[end - stride] - flatCoordinates[offset];
    flatCoordinates = flatCoordinates[end - stride + 1] - flatCoordinates[offset + 1];
    return perimeter += Math.sqrt(dx * dx + flatCoordinates * flatCoordinates);
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, lineStringLength:{enumerable:!0, get:function() {
    return lineStringLength;
  }}, linearRingLength:{enumerable:!0, get:function() {
    return linearRingLength;
  }}});
};

//# sourceMappingURL=module$node_modules$ol$geom$flat$length.js.map
