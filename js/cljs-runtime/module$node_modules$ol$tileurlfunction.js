shadow$provide.module$node_modules$ol$tileurlfunction = function(global, require, module, exports) {
  function createFromTemplate(template, tileGrid) {
    return function(tileCoord, pixelRatio, projection) {
      if (tileCoord) {
        var maxY;
        pixelRatio = tileCoord[0];
        tileGrid && (projection = tileGrid.getFullTileRange(pixelRatio)) && (maxY = projection.getHeight() - 1);
        return (0,require$_DOT__SLASH_uri_DOT_js.renderXYZTemplate)(template, pixelRatio, tileCoord[1], tileCoord[2], maxY);
      }
    };
  }
  function createFromTemplates(templates, tileGrid) {
    const len = templates.length, tileUrlFunctions = Array(len);
    for (let i = 0; i < len; ++i) {
      tileUrlFunctions[i] = createFromTemplate(templates[i], tileGrid);
    }
    return createFromTileUrlFunctions(tileUrlFunctions);
  }
  function createFromTileUrlFunctions(tileUrlFunctions) {
    return 1 === tileUrlFunctions.length ? tileUrlFunctions[0] : function(tileCoord, pixelRatio, projection) {
      if (tileCoord) {
        var h = (0,require$_DOT__SLASH_tilecoord_DOT_js.hash)(tileCoord);
        h = (0,require$_DOT__SLASH_math_DOT_js.modulo)(h, tileUrlFunctions.length);
        return tileUrlFunctions[h](tileCoord, pixelRatio, projection);
      }
    };
  }
  function nullTileUrlFunction(tileCoord, pixelRatio, projection) {
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, createFromTemplate:{enumerable:!0, get:function() {
    return createFromTemplate;
  }}, createFromTemplates:{enumerable:!0, get:function() {
    return createFromTemplates;
  }}, createFromTileUrlFunctions:{enumerable:!0, get:function() {
    return createFromTileUrlFunctions;
  }}, nullTileUrlFunction:{enumerable:!0, get:function() {
    return nullTileUrlFunction;
  }}});
  var require$_DOT__SLASH_math_DOT_js = require("module$node_modules$ol$math"), require$_DOT__SLASH_tilecoord_DOT_js = require("module$node_modules$ol$tilecoord"), require$_DOT__SLASH_uri_DOT_js = require("module$node_modules$ol$uri");
};

//# sourceMappingURL=module$node_modules$ol$tileurlfunction.js.map
