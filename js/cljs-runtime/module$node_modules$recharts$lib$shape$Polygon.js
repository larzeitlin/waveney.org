shadow$provide.module$node_modules$recharts$lib$shape$Polygon = function(global, require, module, exports) {
  function _interopRequireDefault(obj) {
    return obj && obj.__esModule ? obj : {"default":obj};
  }
  function _extends() {
    _extends = Object.assign ? Object.assign.bind() : function(target) {
      for (var i = 1; i < arguments.length; i++) {
        var source = arguments[i], key;
        for (key in source) {
          Object.prototype.hasOwnProperty.call(source, key) && (target[key] = source[key]);
        }
      }
      return target;
    };
    return _extends.apply(this, arguments);
  }
  function _toConsumableArray(arr) {
    var JSCompiler_temp = Array.isArray(arr) ? _arrayLikeToArray(arr) : void 0;
    JSCompiler_temp || (JSCompiler_temp = "undefined" !== typeof Symbol && null != arr[Symbol.iterator] || null != arr["@@iterator"] ? Array.from(arr) : void 0);
    if (!JSCompiler_temp) {
      a: {
        if (arr) {
          if ("string" === typeof arr) {
            JSCompiler_temp = _arrayLikeToArray(arr, void 0);
            break a;
          }
          JSCompiler_temp = Object.prototype.toString.call(arr).slice(8, -1);
          "Object" === JSCompiler_temp && arr.constructor && (JSCompiler_temp = arr.constructor.name);
          if ("Map" === JSCompiler_temp || "Set" === JSCompiler_temp) {
            JSCompiler_temp = Array.from(arr);
            break a;
          }
          if ("Arguments" === JSCompiler_temp || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(JSCompiler_temp)) {
            JSCompiler_temp = _arrayLikeToArray(arr, void 0);
            break a;
          }
        }
        JSCompiler_temp = void 0;
      }
    }
    if (!(arr = JSCompiler_temp)) {
      throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
    }
    return arr;
  }
  function _arrayLikeToArray(arr, len) {
    if (null == len || len > arr.length) {
      len = arr.length;
    }
    for (var i = 0, arr2 = Array(len); i < len; i++) {
      arr2[i] = arr[i];
    }
    return arr2;
  }
  Object.defineProperty(exports, "__esModule", {value:!0});
  exports.Polygon = void 0;
  var _react = _interopRequireDefault(require("module$node_modules$react$index")), _clsx = _interopRequireDefault(require("module$node_modules$clsx$dist$clsx")), _ReactUtils = require("module$node_modules$recharts$lib$util$ReactUtils"), _excluded = ["points", "className", "baseLinePoints", "connectNulls"], isValidatePoint = function(point) {
    return point && point.x === +point.x && point.y === +point.y;
  }, getParsedPoints = function() {
    var points = 0 < arguments.length && void 0 !== arguments[0] ? arguments[0] : [], segmentPoints = [[]];
    points.forEach(function(entry) {
      isValidatePoint(entry) ? segmentPoints[segmentPoints.length - 1].push(entry) : 0 < segmentPoints[segmentPoints.length - 1].length && segmentPoints.push([]);
    });
    isValidatePoint(points[0]) && segmentPoints[segmentPoints.length - 1].push(points[0]);
    0 >= segmentPoints[segmentPoints.length - 1].length && (segmentPoints = segmentPoints.slice(0, -1));
    return segmentPoints;
  }, getSinglePolygonPath = function(points, connectNulls) {
    points = getParsedPoints(points);
    connectNulls && (points = [points.reduce(function(res, segPoints) {
      return [].concat(_toConsumableArray(res), _toConsumableArray(segPoints));
    }, [])]);
    connectNulls = points.map(function(segPoints) {
      return segPoints.reduce(function(path, point, index) {
        return "".concat(path).concat(0 === index ? "M" : "L").concat(point.x, ",").concat(point.y);
      }, "");
    }).join("");
    return 1 === points.length ? "".concat(connectNulls, "Z") : connectNulls;
  };
  exports.Polygon = function(props) {
    var points = props.points, className = props.className, baseLinePoints = props.baseLinePoints, connectNulls = props.connectNulls;
    if (null == props) {
      props = {};
    } else {
      if (null == props) {
        var target = {};
      } else {
        target = {};
        for (var key in props) {
          !Object.prototype.hasOwnProperty.call(props, key) || 0 <= _excluded.indexOf(key) || (target[key] = props[key]);
        }
      }
      var i;
      if (Object.getOwnPropertySymbols) {
        var sourceSymbolKeys = Object.getOwnPropertySymbols(props);
        for (i = 0; i < sourceSymbolKeys.length; i++) {
          key = sourceSymbolKeys[i], 0 <= _excluded.indexOf(key) || Object.prototype.propertyIsEnumerable.call(props, key) && (target[key] = props[key]);
        }
      }
      props = target;
    }
    if (!points || !points.length) {
      return null;
    }
    className = (0,_clsx["default"])("recharts-polygon", className);
    if (baseLinePoints && baseLinePoints.length) {
      return target = props.stroke && "none" !== props.stroke, key = getSinglePolygonPath(points, connectNulls), key = "".concat("Z" === key.slice(-1) ? key.slice(0, -1) : key, "L").concat(getSinglePolygonPath(baseLinePoints.reverse(), connectNulls).slice(1)), _react["default"].createElement("g", {className}, _react["default"].createElement("path", _extends({}, (0,_ReactUtils.filterProps)(props, !0), {fill:"Z" === key.slice(-1) ? props.fill : "none", stroke:"none", d:key})), target ? _react["default"].createElement("path", 
      _extends({}, (0,_ReactUtils.filterProps)(props, !0), {fill:"none", d:getSinglePolygonPath(points, connectNulls)})) : null, target ? _react["default"].createElement("path", _extends({}, (0,_ReactUtils.filterProps)(props, !0), {fill:"none", d:getSinglePolygonPath(baseLinePoints, connectNulls)})) : null);
    }
    points = getSinglePolygonPath(points, connectNulls);
    return _react["default"].createElement("path", _extends({}, (0,_ReactUtils.filterProps)(props, !0), {fill:"Z" === points.slice(-1) ? props.fill : "none", className, d:points}));
  };
};

//# sourceMappingURL=module$node_modules$recharts$lib$shape$Polygon.js.map
