shadow$provide.module$node_modules$recharts$lib$util$CartesianUtils = function(global, require, module, exports) {
  function _interopRequireDefault(obj) {
    return obj && obj.__esModule ? obj : {"default":obj};
  }
  function _typeof(o) {
    "@babel/helpers - typeof";
    return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o) {
      return typeof o;
    } : function(o) {
      return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
    }, _typeof(o);
  }
  function _defineProperties(target, props) {
    for (var i = 0; i < props.length; i++) {
      var descriptor = props[i];
      descriptor.enumerable = descriptor.enumerable || !1;
      descriptor.configurable = !0;
      "value" in descriptor && (descriptor.writable = !0);
      Object.defineProperty(target, _toPropertyKey(descriptor.key), descriptor);
    }
  }
  function _createClass(Constructor, protoProps, staticProps) {
    protoProps && _defineProperties(Constructor.prototype, protoProps);
    staticProps && _defineProperties(Constructor, staticProps);
    Object.defineProperty(Constructor, "prototype", {writable:!1});
    return Constructor;
  }
  function ownKeys(e, r) {
    var t = Object.keys(e);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e);
      r && (o = o.filter(function(r) {
        return Object.getOwnPropertyDescriptor(e, r).enumerable;
      }));
      t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread(e) {
    for (var r = 1; r < arguments.length; r++) {
      var t = null != arguments[r] ? arguments[r] : {};
      r % 2 ? ownKeys(Object(t), !0).forEach(function(r) {
        _defineProperty(e, r, t[r]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function(r) {
        Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
      });
    }
    return e;
  }
  function _defineProperty(obj, key, value) {
    key = _toPropertyKey(key);
    key in obj ? Object.defineProperty(obj, key, {value, enumerable:!0, configurable:!0, writable:!0}) : obj[key] = value;
    return obj;
  }
  function _toPropertyKey(t) {
    a: {
      if ("object" == _typeof(t) && t) {
        var e = t[Symbol.toPrimitive];
        if (void 0 !== e) {
          t = e.call(t, "string");
          if ("object" != _typeof(t)) {
            break a;
          }
          throw new TypeError("@@toPrimitive must return a primitive value.");
        }
        t = String(t);
      }
    }
    return "symbol" == _typeof(t) ? t : t + "";
  }
  function normalizeAngle(angle) {
    return (angle % 180 + 180) % 180;
  }
  Object.defineProperty(exports, "__esModule", {value:!0});
  exports.getAngledRectangleWidth = exports.formatAxisMap = exports.createLabeledScales = exports.ScaleHelper = void 0;
  exports.normalizeAngle = normalizeAngle;
  exports.rectWithPoints = exports.rectWithCoords = void 0;
  var _mapValues = _interopRequireDefault(require("module$node_modules$lodash$mapValues")), _every = _interopRequireDefault(require("module$node_modules$lodash$every")), _ChartUtils = require("module$node_modules$recharts$lib$util$ChartUtils"), _ReactUtils = require("module$node_modules$recharts$lib$util$ReactUtils"), _DataUtils = require("module$node_modules$recharts$lib$util$DataUtils"), _Bar = require("module$node_modules$recharts$lib$cartesian$Bar");
  exports.formatAxisMap = function(props, axisMap, offset, axisType, chartName) {
    var width = props.width, height = props.height, layout = props.layout, children = props.children, ids = Object.keys(axisMap), steps = {left:offset.left, leftMirror:offset.left, right:width - offset.right, rightMirror:width - offset.right, top:offset.top, topMirror:offset.top, bottom:height - offset.bottom, bottomMirror:height - offset.bottom}, hasBar = !!(0,_ReactUtils.findChildByType)(children, _Bar.Bar);
    return ids.reduce(function(result, id) {
      var axis = axisMap[id], orientation = axis.orientation, domain = axis.domain, _axis$padding = axis.padding, padding = void 0 === _axis$padding ? {} : _axis$padding, mirror = axis.mirror, reversed = axis.reversed;
      _axis$padding = "".concat(orientation).concat(mirror ? "Mirror" : "");
      var calculatedPadding;
      if ("number" === axis.type && ("gap" === axis.padding || "no-gap" === axis.padding)) {
        var diff = domain[1] - domain[0], smallestDistanceBetweenValues = Infinity, sortedValues = axis.categoricalDomain.sort();
        sortedValues.forEach(function(value, index) {
          0 < index && (smallestDistanceBetweenValues = Math.min((value || 0) - (sortedValues[index - 1] || 0), smallestDistanceBetweenValues));
        });
        if (Number.isFinite(smallestDistanceBetweenValues)) {
          var smallestDistanceInPercent = smallestDistanceBetweenValues / diff;
          diff = "vertical" === axis.layout ? offset.height : offset.width;
          "gap" === axis.padding && (calculatedPadding = smallestDistanceInPercent * diff / 2);
          "no-gap" === axis.padding && (calculatedPadding = (0,_DataUtils.getPercentValue)(props.barCategoryGap, smallestDistanceInPercent * diff), smallestDistanceInPercent = smallestDistanceInPercent * diff / 2, calculatedPadding = smallestDistanceInPercent - calculatedPadding - (smallestDistanceInPercent - calculatedPadding) / diff * calculatedPadding);
        }
      }
      padding = "xAxis" === axisType ? [offset.left + (padding.left || 0) + (calculatedPadding || 0), offset.left + offset.width - (padding.right || 0) - (calculatedPadding || 0)] : "yAxis" === axisType ? "horizontal" === layout ? [offset.top + offset.height - (padding.bottom || 0), offset.top + (padding.top || 0)] : [offset.top + (padding.top || 0) + (calculatedPadding || 0), offset.top + offset.height - (padding.bottom || 0) - (calculatedPadding || 0)] : axis.range;
      reversed && (padding = [padding[1], padding[0]]);
      calculatedPadding = (0,_ChartUtils.parseScale)(axis, chartName, hasBar);
      reversed = calculatedPadding.scale;
      calculatedPadding = calculatedPadding.realScaleType;
      reversed.domain(domain).range(padding);
      (0,_ChartUtils.checkDomainOfScale)(reversed);
      domain = (0,_ChartUtils.getTicksOfScale)(reversed, _objectSpread(_objectSpread({}, axis), {}, {realScaleType:calculatedPadding}));
      if ("xAxis" === axisType) {
        var needSpace = "top" === orientation && !mirror || "bottom" === orientation && mirror;
        var x = offset.left;
        var y = steps[_axis$padding] - needSpace * axis.height;
      } else {
        "yAxis" === axisType && (needSpace = "left" === orientation && !mirror || "right" === orientation && mirror, x = steps[_axis$padding] - needSpace * axis.width, y = offset.top);
      }
      orientation = _objectSpread(_objectSpread(_objectSpread({}, axis), domain), {}, {realScaleType:calculatedPadding, x, y, scale:reversed, width:"xAxis" === axisType ? offset.width : axis.width, height:"yAxis" === axisType ? offset.height : axis.height});
      orientation.bandSize = (0,_ChartUtils.getBandSizeOfAxis)(orientation, domain);
      axis.hide || "xAxis" !== axisType ? axis.hide || (steps[_axis$padding] += (needSpace ? -1 : 1) * orientation.width) : steps[_axis$padding] += (needSpace ? -1 : 1) * orientation.height;
      return _objectSpread(_objectSpread({}, result), {}, _defineProperty({}, id, orientation));
    }, {});
  };
  var rectWithPoints = exports.rectWithPoints = function(_ref, _ref2) {
    var x1 = _ref.x;
    _ref = _ref.y;
    var x2 = _ref2.x;
    _ref2 = _ref2.y;
    return {x:Math.min(x1, x2), y:Math.min(_ref, _ref2), width:Math.abs(x2 - x1), height:Math.abs(_ref2 - _ref)};
  };
  exports.rectWithCoords = function(_ref3) {
    return rectWithPoints({x:_ref3.x1, y:_ref3.y1}, {x:_ref3.x2, y:_ref3.y2});
  };
  var ScaleHelper = exports.ScaleHelper = function() {
    function ScaleHelper(scale) {
      if (!(this instanceof ScaleHelper)) {
        throw new TypeError("Cannot call a class as a function");
      }
      this.scale = scale;
    }
    return _createClass(ScaleHelper, [{key:"domain", get:function() {
      return this.scale.domain;
    }}, {key:"range", get:function() {
      return this.scale.range;
    }}, {key:"rangeMin", get:function() {
      return this.range()[0];
    }}, {key:"rangeMax", get:function() {
      return this.range()[1];
    }}, {key:"bandwidth", get:function() {
      return this.scale.bandwidth;
    }}, {key:"apply", value:function(value) {
      var _ref4 = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : {}, bandAware = _ref4.bandAware;
      _ref4 = _ref4.position;
      if (void 0 !== value) {
        if (_ref4) {
          switch(_ref4) {
            case "start":
              return this.scale(value);
            case "middle":
              return bandAware = this.bandwidth ? this.bandwidth() / 2 : 0, this.scale(value) + bandAware;
            case "end":
              return bandAware = this.bandwidth ? this.bandwidth() : 0, this.scale(value) + bandAware;
            default:
              return this.scale(value);
          }
        }
        return bandAware ? (bandAware = this.bandwidth ? this.bandwidth() / 2 : 0, this.scale(value) + bandAware) : this.scale(value);
      }
    }}, {key:"isInRange", value:function(value) {
      var range = this.range(), first = range[0];
      range = range[range.length - 1];
      return first <= range ? value >= first && value <= range : value >= range && value <= first;
    }}], [{key:"create", value:function(obj) {
      return new ScaleHelper(obj);
    }}]);
  }();
  _defineProperty(ScaleHelper, "EPS", 1e-4);
  exports.createLabeledScales = function(options) {
    var scales = Object.keys(options).reduce(function(res, key) {
      return _objectSpread(_objectSpread({}, res), {}, _defineProperty({}, key, ScaleHelper.create(options[key])));
    }, {});
    return _objectSpread(_objectSpread({}, scales), {}, {apply:function(coord) {
      var _ref5 = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : {}, bandAware = _ref5.bandAware, position = _ref5.position;
      return (0,_mapValues["default"])(coord, function(value, label) {
        return scales[label].apply(value, {bandAware, position});
      });
    }, isInRange:function(coord) {
      return (0,_every["default"])(coord, function(value, label) {
        return scales[label].isInRange(value);
      });
    }});
  };
  exports.getAngledRectangleWidth = function(_ref6) {
    var width = _ref6.width, height = _ref6.height, angleRadians = normalizeAngle(1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : 0) * Math.PI / 180, angleThreshold = Math.atan(height / width);
    return Math.abs(angleRadians > angleThreshold && angleRadians < Math.PI - angleThreshold ? height / Math.sin(angleRadians) : width / Math.cos(angleRadians));
  };
};

//# sourceMappingURL=module$node_modules$recharts$lib$util$CartesianUtils.js.map
